# Azure DevOps — MCP integration (Section 4 of CLAUDE.md)
ADO_ORG_URL=
ADO_PROJECT=
ADO_PAT=
ADO_WORK_ITEM_POLL_INTERVAL_SECONDS=
# Tag this on parent work items only — child stories use ai-pipeline tag and will never be picked up as triggers
ADO_TRIGGER_TAG=
